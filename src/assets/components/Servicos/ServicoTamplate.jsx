import React from 'react'
import styles from '../../css/servicoTamplate.module.css'
function ServicoTamplate({id}) {
    
  return (
    <div className={`${styles.tamplate}`}>
        <div className={`${styles.tamplateContainer}`}>
           
        </div>
    </div>
  )
}

export default ServicoTamplate